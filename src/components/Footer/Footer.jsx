import React from "react";
import RequiredInfo from "../RequiredInfo";

import "./style.scss";

const Footer = (props) => {
  return (
    <div>
      <RequiredInfo />
    </div>
  );
};

export default Footer;
